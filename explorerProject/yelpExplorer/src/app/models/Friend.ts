export class Friend {
  id: string;
  userId: string;
  fiendId: string;

  constructor(id: string, userId: string, fiendId: string) {
    this.id = id;
    this.userId = userId;
    this.fiendId = fiendId;
  }
}
